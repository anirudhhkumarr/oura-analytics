# Production values live in `.env.production` and the GitHub Pages workflow.
# Local `npm run dev` uses the Vite `/oura-api` proxy automatically.
# Register the page URL as the Oura Redirect URI, e.g.:
#   https://anirudhhkumarr.github.io/oura-analytics/
#   http://localhost:5173/
